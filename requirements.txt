# Soccer Stars Analyzer — Python dependencies
#
# For LOCAL DESKTOP testing only (not used by Buildozer/Android).
# Buildozer pulls its own recipes; see buildozer.spec [requirements].
#
# Install with:  pip install -r requirements.txt

kivy==2.3.0
numpy>=1.26.0
opencv-python-headless>=4.9.0
